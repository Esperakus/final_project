#{{ ansible_managed }}

scrape_configs:
-   job_name: node-exporter
    static_configs:
    -   targets:
{% for host in groups['all'] %}
        - {{ hostvars[host]['inventory_hostname'] }}:9100
{% endfor %}